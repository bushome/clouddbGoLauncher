Replace this folder's contents with the extracted contents of the official
Windows x64 Node.js binary distribution (node.exe plus its accompanying
files), e.g. from a node-v24.2.0-win-x64.zip download.

After replacing, this folder should contain node.exe directly (not nested
inside another subfolder) — main.go looks for:

    <exeDir>\node\node.exe

Delete this placeholder file once the real runtime is in place.
